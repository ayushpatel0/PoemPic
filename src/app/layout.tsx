
import type { Metadata } from 'next';
import { Inter } from 'next/font/google'; // Import Inter font
import './globals.css';
import { Toaster } from "@/components/ui/toaster"; // Import Toaster

// Initialize Inter font with subsets
const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'PoemPic - AI Poem Generator from Photos',
  description: 'Upload an image and get a unique poem generated by AI based on its content and mood.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}> {/* Apply Inter font class */}
        {children}
        <Toaster /> {/* Add Toaster component */}
      </body>
    </html>
  );
}
